package networking;

public class PeerEngine {
	
	//peer directory includes peer address, last latency, last state, time thereof, peer type (list of attributes) 
	
	//when a peer state change is directly detected (connect or drop), notify all peers
	//when indirectly detected ( by notification), ??

}
