package validation;

public class Validator {
	
	//internal consistency
	//validate balances add up
	
	//signature check
	//public key, signature, signed_text, signer_time, signer_message (concatenated)
	
	//get previous links
	
	//previous link consistency check (sender/receiver last transaction, last shared transaction)
	//validate math works out (balance differences)
	//validate times are in the past
	//validate transactions are ordered and no sequence is missing and sequences are unique.
	
	//validate trusted witnesses
	
	//validate civic id
	
	

}
